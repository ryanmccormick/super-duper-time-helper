# Javascript Node CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-javascript/ for more details
#
version: 2

defaults: &defaults
  working_directory: ~/repo
  docker:
    - image: circleci/node:10.15.3

jobs:
  checkout:
    <<: *defaults
    steps:
      - checkout

      - restore_cache:
          keys:
            - v1-dependencies-{{ checksum "package.json" }}
            - v1-dependencies-

      - run: yarn install

      - save_cache:
          paths:
            - node_modules
          key: v1-dependencies-{{ checksum "package.json" }}

      - persist_to_workspace:
          root: ~/repo
          paths: .
  test:
    <<: *defaults
    steps:
      - attach_workspace:
          at: ~/repo
      - run:
          name: Run tests
          command: npm test
          environment:
            JEST_JUNIT_OUTPUT: ~/repo/junit.xml
  deploy:
    <<: *defaults
    steps:
      - attach_workspace:
          at: ~/repo
#      - run:
#          name: Set registry URL
#          command: npm set registry https://registry.npmjs.com/
      - run:
          name: Add token
          command: echo "//registry.npmjs.org/:_authToken=$NPM_TOKEN" >> ~/repo/.npmrc && cat .npmrc
#      - run:
#          name: Add token
#          command: echo "auth = $NPM_TOKEN" >> ~/repo/.npmrc && cat .npmrc
      - run:
          name: Publish package
          command: npm publish

workflows:
  version: 2
  test-deploy:
    jobs:
      - checkout:
          filters:
            tags:
              only: /^v.*/
      - test:
          requires:
            - checkout
          filters:
            tags:
              only: /^v.*/
      - deploy:
          requires:
            - test
          filters:
            branches:
              only: /^master.*/
